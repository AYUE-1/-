spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pet_adoption
    username: postgres
    password: 142500
    driver-class-name: org.postgresql.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000

logging:
  level:
    com.petadoption: debug
